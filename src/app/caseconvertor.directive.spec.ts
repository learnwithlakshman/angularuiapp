import { CaseconvertorDirective } from './caseconvertor.directive';

describe('CaseconvertorDirective', () => {
  it('should create an instance', () => {
    const directive = new CaseconvertorDirective();
    expect(directive).toBeTruthy();
  });
});
